version: "3.8"

services:
  db:
    image: postges
    restart: always
    environment:
      - POSTGRES_DB= test_db
      - POSTGRES_USER= test_user
      - POSTGRES_PASSWORD= root
    ports: -5432:5432
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
    driver: local
